{
  // Install python extension"
  "extensions": [
    "ms-python.python"
  ],

  // Run Bash script in .devcontainer directory
  "postCreateCommand": "/bin/bash ./setup/setup.sh > ~/post-create.log",
}
